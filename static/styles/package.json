{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch style.scss:style.css",
    "autoprefix": "postcss style.css --use autoprefixer --replace",
    "build": "concurrently \"npm run sass\" \"npm run autoprefix\""
  },
  "author": "Boubekeur Jeremy",
  "license": "ISC",
  "browserslist": "last 4 versions",
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "concurrently": "^8.2.2",
    "postcss-cli": "^11.0.0",
    "sass": "^1.75.0"
  },
  "dependencies": {
    "browserlist": "^1.0.1",
    "postcss": "^8.4.38"
  }
}
